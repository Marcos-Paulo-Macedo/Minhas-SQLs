SELECT DISTINCT P.APLICACAO,
       (
        SELECT COUNT(*)
        FROM PRODS P2 
        WHERE P2.APLICACAO <= P.APLICACAO AND P2.APLICACAO IS NOT NULL
        ) AS ID_APLICACAO
FROM PRODS P
WHERE P.APLICACAO IS NOT NULL
ORDER BY P.APLICACAO
